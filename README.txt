# Stealth Miner Installer (ProxyChains tanpa root)

## 🛠 Cara Pakai

1. Ekstrak:
   ```
   unzip stealth-miner-noroot.zip
   cd stealth-miner
   ```

2. Jadikan executable:
   ```
   chmod +x stealth-miner-wrapper.sh
   ```

3. Jalankan:
   ```
   ./stealth-miner-wrapper.sh
   ```

📌 File `proxychains.conf` disimpan lokal dan tidak perlu akses root. Pastikan SOCKS5 proxy aktif di `127.0.0.1:9050`.

Jika proxy tidak aktif, mining tidak akan berjalan.
